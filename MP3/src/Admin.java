public class Admin extends User{
    //test feature branch better version test test
    public Admin(String firstName, String lastName, String email) {
        super(firstName, lastName, email);
    }
}
